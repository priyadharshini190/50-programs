using System;

class Program
{
    static void Main()
    {
        int n = 10, a = 0, b = 1;
        Console.WriteLine("Fibonacci Series:");
        Console.WriteLine(a);
        Console.WriteLine(b);
        for (int i = 3; i <= n; i++)
        {
            int next = a + b;
            Console.WriteLine(next);
            a = b;
            b = next;
        }
    }
}
